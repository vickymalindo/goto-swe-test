import styled from '@emotion/styled';

// interface IconPinProps {
//   color: string;
// }

export const IconSearch = styled.span`
  display: inline-block;
  color: #eaeaea;
  font-size: 24px;
`;

export const IconPin = styled.span`
  display: inline-block;
  font-size: 20px;
  /* color: ${(props) => props.color}; */
`;
