import { HeaderTitle, TitleText } from '../atoms/Headers';

const Header = () => {
  return (
    <HeaderTitle>
      <TitleText>WartelQ</TitleText>
    </HeaderTitle>
  );
};

export default Header;
